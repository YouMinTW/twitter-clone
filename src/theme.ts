type Theme = {
  main: {
    color: string;
    backgroundColor: string;
  };
};

const theme: Theme = {
  main: {
    color: '#50b7f5',
    backgroundColor: '#e6ecf0',
  },
};

export default theme;
